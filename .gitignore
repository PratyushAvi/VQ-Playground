target/
node_modules/
dist/
pkg/
*.bin
.DS_Store

# Regenerated by tools/make_fixture.py -- deterministic, so not worth tracking.
tools/fixtures/

# The vq-bench fork. Not a submodule yet: that needs a fork URL you own.
# See README "Getting the fork" for how to (re)create it.
vendor/vq-bench/
